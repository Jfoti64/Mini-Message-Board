extends layout

block content
  h1= title
  p Welcome to #{title}

  ul
      each message in messages
        li
          strong= message.user
          |  said: 
          |  #{message.text}
          |  (added on #{message.added.toDateString()})

  button(type="button" onclick="location.href='/new'") Add new message
    
